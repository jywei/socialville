<%= link_to user_path(user.username) do %>
  <%= image_tag user.image.url(:thumb), size: "100x100", class: "img-rounded" %>
<% end %>

<h3><%= user.username %></h3>

<% if current_user %>
  <p><%= action_buttons(user)%></p>
<% end %>
